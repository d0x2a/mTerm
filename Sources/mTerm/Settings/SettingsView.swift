import SwiftUI

/// One-pane Settings for now. When other categories (General / Advanced /
/// Profiles) grow real content, re-introduce a NavigationSplitView sidebar.
struct SettingsView: View {
    var body: some View {
        AppearancePane()
    }
}
